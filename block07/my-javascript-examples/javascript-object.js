
const object={
    key:"value"
}

const amaniProfile = {
    //NESTED STRING
    //name:{ 
       // firstName:"Amani",
        //lastName:"Taylor",
    firstName:"Amani",
    lastName:"Taylor",
    hobby:"goes to the movies",
    favoriteFood:"cookies",
    imaginaryPet:"pegasus",
    age:"34"

}
console.log(amaniProfile.age,amaniProfile.firstName)

//use backtick (`) and dollar ($)
console.log(`${amaniProfile.firstName} likes to ${amaniProfile.hobby}`)
console.log(`${amaniProfile.firstName} loves her ${amaniProfile.imaginaryPet}`)
console.log(`${amaniProfile.firstName} ${amaniProfile.hobby} with her ${amaniProfile.imaginaryPet}`)

//Create a program that takes two numbers as inputs and outputs the sum of those numbers.
let = value1, value2 
{
    return value1 + value2
};

let = 8, 2;
{
    return 8+2
};

//Write a function that takes a number as input and checks if it is even or odd. If the number is even, return true; if it is odd, return false.

function evenOdd (num1)
{
if (num1) {
    
}
}